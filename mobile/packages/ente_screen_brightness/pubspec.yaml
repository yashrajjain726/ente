name: ente_screen_brightness
description: iOS screen brightness control for Ente Flutter apps

resolution: workspace
environment:
  sdk: ">=3.10.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_lints: 6.0.0

flutter:
  plugin:
    platforms:
      ios:
        pluginClass: EnteScreenBrightnessPlugin
