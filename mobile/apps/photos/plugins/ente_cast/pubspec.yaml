name: ente_cast

resolution: workspace
environment:
  sdk: ">=3.10.0 <4.0.0"
  flutter: ">=3.38.0"

dependencies:
  flutter:
    sdk: flutter
  multicast_dns: 0.3.3+1

dev_dependencies:
  flutter_lints: 6.0.0
  flutter_test:
    sdk: flutter

flutter:
  plugin:
    platforms:
      android:
        package: io.ente.cast
        pluginClass: EnteCastPlugin
      ios:
        pluginClass: EnteCastPlugin
